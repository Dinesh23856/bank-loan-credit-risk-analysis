import {useState} from "react"; import {Link,useNavigate} from "react-router-dom"; import {ShieldCheck} from "lucide-react"; import {register} from "../api/client"; import ErrorMessage from "../components/ErrorMessage";
export default function Register(){const nav=useNavigate(),[form,setForm]=useState({name:"",email:"",password:"",confirm:""}),[err,setErr]=useState(""),[busy,setBusy]=useState(false);
async function submit(e){e.preventDefault();setErr("");if(form.password!==form.confirm){setErr("Passwords do not match.");return}setBusy(true);try{await register({name:form.name,email:form.email,password:form.password});nav("/login")}catch(e){setErr(e.message)}finally{setBusy(false)}}return <section className="auth card"><ShieldCheck size={42}/><h1>Create your account</h1><form onSubmit={submit}><input required minLength="2" maxLength="120" placeholder="Full name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input required type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input required minLength="8" maxLength="128" type="password" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><input required type="password" placeholder="Confirm password" value={form.confirm} onChange={e=>setForm({...form,confirm:e.target.value})}/><ErrorMessage message={err}/><button className="primary" disabled={busy}>{busy?"Creating…":"Register"}</button><p>Already registered? <Link to="/login">Sign in</Link></p></form></section>}