import {useState} from "react"; import {Link,useNavigate} from "react-router-dom"; import {ShieldCheck} from "lucide-react"; import {login} from "../api/client"; import ErrorMessage from "../components/ErrorMessage";
export default function Login(){const nav=useNavigate(),[form,setForm]=useState({email:"",password:""}),[err,setErr]=useState(""),[busy,setBusy]=useState(false);
async function submit(e){e.preventDefault();setBusy(true);setErr("");try{const r=await login(form);localStorage.setItem("access_token",r.access_token);nav(r.role==="admin"?"/admin":"/dashboard",{replace:true})}catch(e){setErr(e.message)}finally{setBusy(false)}}return <section className="auth card"><ShieldCheck size={42}/><h1>Welcome back</h1><form onSubmit={submit}><input required type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input required type="password" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><ErrorMessage message={err}/><button className="primary" disabled={busy}>{busy?"Signing in…":"Sign in"}</button><p>New user? <Link to="/register">Create account</Link></p></form></section>}